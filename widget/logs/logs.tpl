
    {
      "type": "${type}",
      "x": ${x},
      "y": ${y},
      "width": ${width},
      "height": ${height},
      "properties": {
        "region": "${region}",
        "title": "${title}",
        "query": "${query}"
      }
    }

