
    {
        "type": "${type}",
        "x": ${x},
        "y": ${y},
        "width": ${width},
        "height": ${height},
        "properties": {
            "metrics": [
                [
                    "${metrics_namespace}",
                    "${metrics_name}",
                    "${instance_id_name}",
                    "${instance_id}"
                ]
            ],
            "period": ${period},
            "stat": "${stat}",
            "region": "${region}",
            "title": "${title}"
        }
    }
